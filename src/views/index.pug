doctype html
html
head
meta(charset='utf-8')
link(rel='stylesheet' href='styles.css')
tittle Consultas SQL
  body
  h1(align="center") Consulta
  form.tabla(method='post' action='/' align='center')
  textarea(autofocus='' name='query' rows='10' cols='60')
  br
  input.boton(type='submit')

  if (results)
  br
  table( border="black" class="tabla" align="center" )
  thead
  each col in fields
  th=col.name
  tbody
  each row in results
  tr
  each col in fields
  td=row[col.name]
